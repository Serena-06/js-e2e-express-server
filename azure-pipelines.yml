trigger:
  - main

pool:
  name: 'MiAgente-Pool'

steps:
  - script: |
      echo "Hello from Ubuntu Agent!"
      uname -a
      whoami
    displayName: 'Run on Self-Hosted Agent'

  - script: |
      echo "Building project..."
    displayName: 'Build'